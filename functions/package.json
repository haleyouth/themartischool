{
  "name": "themartischool-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for The Marti School",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "npm run clean && tsc",
    "build:watch": "tsc --watch",
    "deploy": "npm run build && node -e \"process.env.FUNCTIONS_DISCOVERY_TIMEOUT='60';require('child_process').execSync('firebase deploy --only functions',{stdio:'inherit'})\"",
    "logs": "firebase functions:log",
    "clean": "node -e \"require('fs').rmSync('lib',{recursive:true,force:true})\""
  },
  "dependencies": {
    "dejavu-fonts-ttf": "^2.37.3",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^7.3.2",
    "pdfkit": "^0.19.1"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.6",
    "typescript": "^5.7.3"
  },
  "private": true
}
